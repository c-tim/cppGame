#include <gameManager.hpp>
#include <gameEvents.hpp>

using std::cout;

//gameManager* gameEvent::instance;

/*static void gameEvent::setInstance(gameManager& gmInstance);

static bool gameEvent::EventNewCropPlanted(sf::Vector2f posCrop);
*/
/*void gameEvent::setInstance(gameManager* gmInstance) {
  if (instance != nullptr) {
    cout << "ERROR instance gameManager already defined\n";
  }
  instance = gmInstance;
}*/

/*bool gameEvent::EventNewCropPlanted(sf::Vector2f posCrop) {
  return instance->newCropPlanted(posCrop);
}
*/